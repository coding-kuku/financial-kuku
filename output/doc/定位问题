程序问题定位

1. 科目余额表接口与 CLI 的时间参数约定不一致

现象：
调用 `ledger balance` 时返回 500。

根因：
CLI 把 `ledger balance --start/--end` 定义为自然日期 `YYYY-MM-DD`，但后端 `/financeCertificate/queryDetailBalanceAccount` 实际按会计期间 `yyyyMM` 处理。请求传入 `2021-01-01` 这类值后，后端会在期间解析阶段报错。

代码位置：
- CLI 参数定义：`agent-harness/cli_anything/wukong/wukong_cli.py:793-796`
- CLI 请求体构造：`agent-harness/cli_anything/wukong/core/ledger.py:76-81`
- 后端余额表入口：`finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:156-160`
- 后端余额表实现，直接把 `startTime/endTime` 当会计期间使用：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:632-667`
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:717-745`
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:755-782`

结论：
这是接口契约问题。CLI 与后端对同一接口的时间格式定义不一致，导致调用直接触发服务端解析异常。

2. 资产负债表校验接口会把辅助核算编号当成数字解析

现象：
调用 `report balance-sheet --check` 时返回 500。

根因：
余额表校验路径会先取科目余额，再按科目编码范围过滤。辅助核算科目在构造余额结果时，会把辅助卡片编号拼进 `number` 字段，例如 `1122_C0002`。后续过滤逻辑调用 `RuleUtils.fillUpNumber` 时只去掉下划线，不去掉字母，最终执行 `new BigDecimal("1122C0002")`，因此抛出数字解析异常。

代码位置：
- 资产负债表校验入口：`finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceBalanceSheetReportServiceImpl.java:516-593`
- 校验时调用余额表：`finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceBalanceSheetReportServiceImpl.java:538-541`
- 辅助核算余额结果拼接 `number`：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:1152-1216`
  关键行为在 `1174-1178`
- 余额表按编码过滤：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:540-575`
- 数字补齐工具：
  `finance/finance-web/src/main/java/com/kakarote/finance/common/RuleUtils.java:72-106`

结论：
这是后端过滤逻辑对辅助核算编码兼容性不足。辅助卡片编码进入 `number` 后，被错误地按纯数字科目编码处理。

3. 有子科目时，父科目本身直接记账的余额会被漏算

现象：
资产负债表部分项目取数不正确。典型表现是一级科目下面已经有二级科目时，如果凭证又直接记在一级科目本身，则报表可能只汇总子科目，不包含父科目直营余额。

根因：
报表取数和余额汇总都依赖“只取第一层子科目”的逻辑。只要某科目存在子科目，程序就走“汇总子科目”分支，不再把父科目本身的发生额和余额纳入计算。这样会导致“父级有子级，同时父级自己也被直接使用”的场景漏数。

代码位置：
- 资产负债表按第一层子科目取值：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceBalanceSheetReportServiceImpl.java:301-370`
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceBalanceSheetReportServiceImpl.java:404-475`
- 第一层子科目方法本身不包含父科目自身：
  `finance/finance-web/src/main/java/com/kakarote/finance/common/CashFlowStatementReportHolder.java:110-112`
- 科目余额计算时，只要存在子科目就只汇总子科目：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:2968-3132`
  关键分支在 `2972-2975`、`3014-3020`、`3044-3077`
- 科目树汇总时，父级也是基于子级结果继续累加：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:1240-1355`

结论：
这是余额汇总模型的问题。程序默认“非末级科目只做汇总、不允许自己有余额”，但当前账套数据允许父科目直接记账，因此会出现报表漏数。

维护建议

1. 统一 `/financeCertificate/queryDetailBalanceAccount` 的时间参数契约，明确到底使用 `yyyyMM` 还是 `YYYY-MM-DD`，并同步 CLI 与后端校验。
2. 在余额表过滤和排序逻辑中，不要把包含辅助核算卡片编号的 `number` 直接按纯数字转 `BigDecimal`。
3. 调整余额与报表汇总逻辑，父科目存在子科目时，也应同时纳入父科目自身直接记账的数据，或在记账层明确禁止这种数据进入。

4. 凭证录入时新增二级科目，会把“父子科目类别不一致”误报成“科目编码重复”

现象：
在凭证录入页通过“新建科目”新增二级科目时，系统提示“科目编码重复，不能添加!”，但实际输入的编码并未重复。

已复现样例：
- 在 `1601 固定资产` 下新增 `160101`，若请求参数为 `type=1, category=1`，接口返回 `7010 科目编码重复，不能添加!`；改为 `category=2` 后可正常新增。
- 在 `5711 营业外支出` 下新增 `571111`，若请求参数为 `type=5, category=1`，接口返回 `7010 科目编码重复，不能添加!`；改为 `category=4` 后可正常新增。

根因一：前端在“凭证录入 -> 新建科目”场景下，会错误推导二级科目的 category

问题路径：
- 凭证录入页使用科目选择组件，并允许直接新建科目：
  `ux/src/views/fm/voucher/Create.vue:250-269`
- 科目选择组件打开新建科目弹窗：
  `ux/src/views/fm/components/subject/SubjectSelectOptions.vue:126-131`
- 新建科目弹窗在根据编码推导父科目时，只拿到了父科目的 `type`，然后把 `category` 强制设置为该类型的第一个类别：
  `ux/src/views/fm/components/subject/SubjectUpdateDialog.vue:639-681`
  `ux/src/views/fm/components/subject/SubjectUpdateDialog.vue:696-701`

具体问题：
- `getParentNodeId()` 找到父科目后调用 `setCategory(item.type)`
- `setCategory(type)` 并不是按父科目的真实 `category` 赋值，而是取 `categoryOptions[0]`
- 因此对于非首类别父科目，会把新建子科目的 category 组装错

影响范围：
- 非流动资产、非流动负债
- 损益类中的其他收益、期间费用、其他损失、营业成本及税金、以前年度损益调整、所得税等非首类别科目

结论：
前端不是把编码算重了，而是把新建子科目的 `category` 传错了。

根因二：后端把“类别/类型不一致”错误错误映射成“编码重复”

问题位置：
- 后端新增科目入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceSubjectController.java:56-68`
- 后端保存逻辑：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceSubjectServiceImpl.java:115-130`

具体问题：
- 当 `parentId` 不为空时，后端先校验“当前科目的 `category/type` 是否与父科目一致”
- 这段逻辑在不一致时抛出的却是 `FINANCE_SUBJECT_NUMBER_ERROR`
- 该错误码对应提示文案是“科目编码重复，不能添加!”

相关错误码定义：
- 当前实际抛出的错误码：
  `finance/finance-web/src/main/java/com/kakarote/finance/constant/FinanceCodeEnum.java:25`
- 实际更符合当前场景的错误码已经存在：
  `finance/finance-web/src/main/java/com/kakarote/finance/constant/FinanceCodeEnum.java:41`
  即 `FINANCE_SUBJECT_TYPE_ERROR(7026, "科目与上级科目，科目类别或科目类型不一致，不能添加!")`

结论：
后端真实失败原因不是“编码重复”，而是“父子科目类别/类型不一致”；只是抛错码用错了，导致前端显示成了误导性的重复提示。

综合判断

这是一个前后端叠加问题：

1. 前端在凭证录入页新增二级科目时，可能把 `category` 自动带错。
2. 后端收到错误的 `category` 后，本应提示“类别/类型不一致”，却错误提示成“编码重复”。

因此用户看到的是“明明没重复却提示重复”，本质上并不是判重失效，而是参数推导错误 + 错误码映射错误。

5. CLI 的 `account create` 命令实际不可用

现象：
执行 `cli-anything-wukong account create --name ... --company ... --start 2026-04` 时，命令未返回业务错误，而是直接抛出 Python `TypeError` 退出。

实测报错：
- `TypeError: create_account() takes from 2 to 3 positional arguments but 5 were given`

根因：
CLI 命令层定义了 `--name --company --start --currency` 四个参数，但调用底层方法时，传参与底层函数签名不一致。命令层把 `name, company, start, currency` 全部透传给 `_account.create_account(...)`，而底层实现只接受 `company_name, start_time`。

代码位置：
- CLI 账套创建命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:356-375`
- CLI 底层账套创建实现：
  `agent-harness/cli_anything/wukong/core/account.py:32-52`

结论：
这是 CLI 自身的参数桥接错误，不是后端业务校验失败。当前命令无法用于创建隔离测试账套。

6. CLI 显示“已切换账套”，但后续查询仍落在原账套

现象：
执行 `cli-anything-wukong account switch 1` 后：
- CLI 返回 `{"account_id": 1, "switched": true}`
- `status` 和本地 session 也显示当前账套为 `1`

但紧接着查询 `statement status`，返回的仍是原测试账套：
- `accountId = 2036268933673287680`
- `settleTime = 2021-01-01 00:00:00`

也就是说，CLI 的本地“当前账套”状态已经切换，但后续业务查询实际仍在旧账套上下文中执行。

代码位置：
- CLI 账套切换命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:335-353`
- CLI 底层账套切换实现：
  `agent-harness/cli_anything/wukong/core/account.py:27-30`
- 后端账套切换入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceAccountSetController.java:109-113`
- 后端账套切换实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAccountSetServiceImpl.java:443-455`

当前定位：
CLI 本地 session 与后续接口实际生效的账套上下文不一致。问题可能在后端切换后的上下文持久化/恢复链路，也可能在当前登录态下切换结果未被后续请求沿用，仍需继续往服务端账套上下文恢复逻辑排查。

结论：
当前 CLI 不能可靠地把后续操作隔离到目标账套，账套切换结果不可信。

7. CLI 通过接口更新凭证时返回 500

现象：
使用 CLI 新增一张最小测试凭证后，调用 `certificate update` 更新摘要、金额和日期，接口返回 500，CLI 显示：
- `API error 500: 网络错误，请稍候再试`

实测过程：
- 先成功新增测试凭证
- 再执行 `certificate update --id ... --voucher-id ... --date 2021-01-29 --detail ...`
- 更新失败
- 随后再次 `get`，确认原凭证未被偷偷改写
- 测试凭证已删除，账套已清理

日志线索：
后台日志落出：
- `TooManyResultsException: Expected one result (or null) to be returned by selectOne(), but found: 9`

根因：
凭证保存/更新共用 `saveAndUpdate()`。更新路径里会先调用 `queryByTime(certificate)` 做同月同凭证字判重，但该查询：
- 没有限制 `account_id`
- 当 `certificateNum` 为空时，也不会按凭证号精确过滤
- 最终只按 `voucher_id + 月份` 查询

因此在同一凭证字同月存在多张凭证时，`selectOne()` 会命中多条，直接抛 `TooManyResultsException`，导致更新接口返回 500。

代码位置：
- 更新入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:88-98`
- 保存/更新共用逻辑：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:152-185`
- 更新前调用的判重查询：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:170-175`
- 判重 SQL：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceCertificateMapper.xml:1446-1456`
- Mapper 方法定义：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/FinanceCertificateMapper.java:68`

结论：
这是后端更新判重逻辑的问题，不是 CLI 请求格式错误。当前只要目标月份同凭证字下存在多张凭证，更新接口就可能直接报 500。

8. CLI 的 `account init` 帮助文案与实际行为高度不一致，存在误清库风险

现象：
CLI 帮助文案把 `account init` 描述为“初始化当前账套默认数据（subjects, voucher words）”，看起来像是给当前账套补默认科目和凭证字。

但实际调用的却是后端 `/financeAccountSet/initFinanceData`，其实现会遍历全部财务表并执行全表清空。

代码位置：
- CLI 帮助与调用入口：
  `agent-harness/cli_anything/wukong/wukong_cli.py:377-388`
- CLI 底层实现注释已明确写成“Clear all finance data tables (destructive reset)”：
  `agent-harness/cli_anything/wukong/core/account.py:79-85`
- 后端接口入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceAccountSetController.java:122-126`
- 后端实际实现，遍历 `FinanceDatabaseEnum` 全量清表：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAccountSetServiceImpl.java:476-486`

当前判断：
该问题本次未实调用，避免误删除现有测试数据；但从 CLI 文案、底层注释和后端实现对照看，命令语义与实际行为明显冲突，存在很高的误操作风险。

结论：
这是一个高风险 CLI 设计问题。即使还未实际触发，也应优先修正文案和命令映射，避免用户把“初始化当前账套”误用成“清空全部财务数据”。

9. CLI 的凭证新增/更新帮助文案把贷方字段写成了 `ownerBalance`

现象：
CLI 的 `certificate add` / `certificate update` 帮助文案和示例 JSON 都写的是 `ownerBalance`，但按帮助文案原样传参时，CLI 校验会把该字段当作不存在，进而报：
- `第 2 条：有科目但借贷金额均为 0`

实测说明：
- 使用 `creditBalance` 传贷方金额时，可正常新增凭证
- 改按帮助文案传 `ownerBalance` 后，第二条分录会被视为“金额全为 0”

根因：
CLI 帮助文案仍沿用了 `ownerBalance` 的旧字段名，但底层校验和请求构造实际只识别 `creditBalance`。

代码位置：
- CLI 新增凭证帮助文案：
  `agent-harness/cli_anything/wukong/wukong_cli.py:667-672`
- CLI 更新凭证帮助文案：
  `agent-harness/cli_anything/wukong/wukong_cli.py:757-762`
- CLI 凭证详情校验只读取 `debtorBalance/creditBalance`：
  `agent-harness/cli_anything/wukong/core/certificate.py:42-89`

结论：
这是 CLI 文案与实际实现不一致的问题。用户按帮助文案操作时，会直接触发错误校验，无法正常录入贷方分录。

10. `certificate get` 返回的审核状态不可信

原始描述（来自用户反馈）：
凭证详情接口返回的 `checkStatus` 与列表筛选实际状态不同步，不能作为审核状态判断依据。

2026-03-29 实测结论（未能复现原始现象）：

测试方式：在隔离测试账套（切换测试账套A）新建测试凭证，逐步执行审核、反审核，对比 `certificate list` 与 `certificate get` 的 `checkStatus`。

测试结果：
- 创建后：list = 0，get = 0，一致
- 审核后：list = 1，get = 1，一致
- 反审核后：list = 0，get = 0，一致

两个接口的 `checkStatus` 在本次测试中始终保持一致。后端 list 接口（`queryPage`）和 get 接口（`queryById`）都从同一 DB 字段 `check_status` 读取，没有发现组装差异。

追加发现：
- `isStatement`（期间是否已结账）在 `certificate get` 中有返回，但 `certificate list` 不返回该字段
- 结账后仍可反审（`--unapprove`），反审后可正常删除——这是正确的业务逻辑，不是 bug
- `certificate get` 的 CLI 输出目前不展示 `checkStatus` 和 `isStatement`，用户无法从 get 结果判断凭证的审核状态和所在期间是否已结账

待跟进：
原始现象（list 与 get 不一致）在当前版本未能复现。需等待用户提供具体复现步骤或环境信息后再排查。

代码位置：
- 后端审核写入：`FinanceCertificateServiceImpl.java:304`（`lambdaUpdate().set(checkStatus, status)`）
- 后端 get 实现：`FinanceCertificateServiceImpl.java:388-435`
- 后端 list 实现：`FinanceCertificateServiceImpl.java:246-294`

11. `ledger general` 返回空数组，但同科目的明细账和报表都有数据

现象：
使用 CLI 查询总账时，多个有实际发生额和余额的科目直接返回空数组 `[]`。

已实测样例：
- `1001 库存现金`
- `2202 应付账款`

对照结果：
- `ledger general --subject-id ... --start 2021-01-01 --end 2021-01-31` 返回 `[]`
- 但同时间段：
  - `ledger detail` 能查到凭证流水
  - `report balance-sheet` 能查到期初/期末余额
  - 例如 `2202 应付账款` 的明细账期末余额为 `-24690.65`，资产负债表中也被取到

根因：
CLI 总账命令调用的是后端 `/financeCertificate/queryDetailUpAccount`，当前看更可能是后端总账查询链路本身有问题，而不是 CLI 参数没传到，因为 CLI 只做了简单透传。

代码位置：
- CLI 总账命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:825-842`
- CLI 总账请求构造：
  `agent-harness/cli_anything/wukong/core/ledger.py:47-55`
- 后端总账入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:150-154`
- 后端总账服务实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:487-536`
- 后端总账 SQL：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceCertificateMapper.xml:508-620`

结论：
当前总账接口存在明显可用性问题。即使科目已有明细和报表余额，总账查询仍可能直接返回空结果。

12. 技能文档中的 `adjuvant add --label project` 示例不能在真实 CLI 中执行

现象：
技能文档示例写的是：
- `cli-anything-wukong adjuvant add --name "研发项目A" --label project`

但真实 CLI 参数定义要求 `--label` 必须是整数。按文档执行会被 Click 直接拒绝：
- `Error: Invalid value for '--label': 'project' is not a valid integer.`

根因：
技能文档把辅助核算标签写成了字符串别名，但 CLI 实现只接受数字枚举：
- `1=客户 2=供应商 3=职员 4=项目 5=部门 6=存货 7=自定义`

代码位置：
- 错误示例来源：
  `agent-harness/WUKONG.md:116-117`
- CLI 参数定义：
  `agent-harness/cli_anything/wukong/wukong_cli.py` 中 `adjuvant add`
- CLI 帮助输出已明确要求整数标签

结论：
这是技能文档/使用说明错误，不是后端故障。但会直接误导测试流程和真实用户操作。

13. `certificate next-num` 返回的“下一个凭证号”明显错误

现象：
调用 `cli-anything-wukong certificate next-num` 查询某月某凭证字的下一个凭证号时，接口会错误返回 `1`，与当前账套中已有凭证数据不符。

已实测样例：
- `voucherId = 2036268933740396548`（`转`），`date = 2021-01-31`
  - 当月实际已有 `转_1` 到 `转_9`
  - `next-num` 仍返回 `{"certificateNum": 1}`
- `voucherId = 2036268933740396545`（`收`），`date = 2021-01-21`
  - 当日实际已有 `收_1`、`收_2`
  - `next-num` 仍返回 `{"certificateNum": 1}`

根因：
CLI 传给 `/financeCertificate/queryNumByTime` 的 `certificateTime` 会被规范成 `yyyyMM`，后端再调用 `queryByTime(certificate)` 查“同月最后一张凭证”。

但该查询 SQL 使用：
- `DATE_FORMAT(certificate_time,'%Y%m') = DATE_FORMAT(#{data.certificateTime},'%Y%m')`

当传入值本身就是 `202101` 这类纯期间字符串时，`DATE_FORMAT(#{data.certificateTime},'%Y%m')` 在 MySQL 中并不能稳定按预期得到有效月份日期，最终导致查询不到已有凭证，接口退回默认值 `1`。

代码位置：
- CLI 命令入口：
  `agent-harness/cli_anything/wukong/wukong_cli.py:733-746`
- CLI 底层请求构造，会把日期规范成 `yyyyMM`：
  `agent-harness/cli_anything/wukong/core/certificate.py:216-237`
- 后端接口入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:176-180`
- 后端实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:2845-2853`
- 后端依赖的判重/取号 SQL：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceCertificateMapper.xml:1446-1456`

结论：
当前 `next-num` 结果不可信，不能用于可靠地预取下一个凭证号。

14. `certificate get` 返回的字段明显不完整

现象：
调用 `cli-anything-wukong certificate get <id>` 查询现有凭证详情时，返回结果中多个应有字段稳定为空。

已实测样例：
- 对已有的 `收_1`、`转_1` 等凭证调用 `certificate get`
- 返回中出现：
  - `voucherName = null`
  - `voucherNum = null`
  - 明细行中的 `subjectName = null`

但同一张凭证通过 `certificate list` 查询时，上述字段都能正常返回，例如：
- `voucherName = 收 / 转`
- `voucherNum = 1 / 9`
- `subjectName = 银行存款 / 应收账款 / 管理费用-社保费` 等

根因：
后端详情接口 `queryById()` 只是：
- 直接 `getById(certificateId)`
- 再单独查明细表

但没有像分页列表接口 `queryPage()` 那样补齐：
- 凭证字名称
- `voucherNum`
- 明细行科目名称
- 附件列表等展示字段

因此详情接口返回的是“基础 PO + 明细”，而不是前端列表那种完整 VO。

代码位置：
- 后端详情入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:134-138`
- 后端详情实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:382-427`
- 对照：列表接口会补全名称和附件等字段：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:240-287`

结论：
凭证详情接口返回内容不完整，不能与列表接口保持一致，会影响 CLI 或其他调用方做详情展示和后续处理。

15. 凭证字新增接口允许重名

现象：
当前账套中本来已经存在默认凭证字 `记`，再次执行：
- `cli-anything-wukong voucher add 记`

接口仍返回成功，并创建出第二个名称同样为 `记` 的凭证字。

实测结果：
- 原默认凭证字：
  - `voucherId = 2036268933740396544`
  - `voucherName = 记`
- 新增出的重复凭证字：
  - `voucherId = 2037840031321821184`
  - `voucherName = 记`
- 该测试数据已删除，账套已清理

根因：
当前至少可以确认，凭证字新增链路没有拦截名称重复。无论是后端未做唯一校验，还是数据库未加唯一约束，结果上都允许在同一账套下创建重名凭证字。

代码位置：
- CLI 新增凭证字命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py` 中 `voucher add`
- 后端凭证字新增链路：
  需继续从 `FinanceVoucherController` / `FinanceVoucherServiceImpl` 追踪具体验证位置

结论：
当前系统允许重复凭证字名称，存在明显数据约束缺失问题，可能影响后续凭证录入、筛选和展示。

16. `report cash-flow --check` 会返回 500

现象：
调用现金流量表接口时：
- `report cash-flow --period month --date 2021-01-31` 可正常返回报表数据
- 但加上 `--check` 后：
  - `report cash-flow --period month --date 2021-01-31 --check`
  - 直接返回 `API error 500: 网络错误，请稍候再试`

根因：
现金流量表的平衡检查实现并不是独立校验，而是直接调用资产负债表的 `balanceCheck(requestBO)`。因此只要资产负债表校验链路在当前期间触发异常，现金流量表 `--check` 也会同步报 500。

从日志调用链可见：
- `FinanceCashFlowStatementReportImpl.balanceCheck(...)`
  -> `balanceSheetService.balanceCheck(requestBO)`
  -> 已知的资产负债表校验异常链路

因此该问题更准确地说，是“现金流量表校验接口直接复用了脆弱的资产负债表校验链路”，导致错误向外扩散。

代码位置：
- CLI 现金流量表命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:976-999`
- CLI 校验调用：
  `agent-harness/cli_anything/wukong/core/report.py:79-81`
- 后端现金流量表校验入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceReportController.java:91-95`
- 后端现金流量表校验实现，直接调用资产负债表校验：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCashFlowStatementReportImpl.java:274-286`

结论：
`cash-flow --check` 当前不可独立信任。它会因为资产负债表校验链路出错而一起返回 500。

17. `ledger general --subject-id` 实际不会按科目过滤

现象：
调用总账命令时，虽然 CLI 要求传入 `--subject-id`，但返回结果并不是“这个科目及其下级科目”的总账，而是整本总账的一个大集合。

已实测样例：
- `ledger general --subject-id 2036268933799116800 --start 2021-01 --end 2021-01 --min-level 1 --max-level 9`
- `ledger general --subject-id 2036268933845254146 --start 2021-01 --end 2021-01 --min-level 1 --max-level 9`

两次返回 JSON 完全一致：
- 都是 `105` 行
- 第一批结果同时包含 `1001`、`1002`、`1122`、`560303` 等彼此无关的科目
- 说明传入的 `subject-id` 没有真正生效

根因：
后端总账实现 `queryDetailUpAccount()` 会先调用 `financeSubjectService.queryIds(accountBO)` 取一批候选科目，再逐个拼装结果。

但这一步依赖的 SQL `querySubjectByIds` 并没有使用传入的 `subjectId` 做过滤，只按：
- `minLevel / maxLevel`
- `startSubjectId / endSubjectId`
- 当前账套

来筛选科目。

因此只要级次范围相同，不同 `subject-id` 最终都会跑出同一批总账结果。

代码位置：
- CLI 总账请求体构造：
  `agent-harness/cli_anything/wukong/core/ledger.py:47-67`
- 后端总账实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:493-543`
- 后端取候选科目的 SQL，未按 `subjectId` 过滤：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceSubjectMapper.xml:89-105`

结论：
当前 `ledger general --subject-id` 返回结果不可信。业务上会把“单科目总账”错误变成“整本总账切片”，无法用于可靠地核对某一个科目的汇总数据。

18. `ledger balance` 的 `--subject-id` 与 `--level` 过滤都会失效

现象：
科目余额表命令虽然支持：
- `--subject-id`
- `--level`

但这两个筛选条件在当前实现中都没有真正生效。

已实测样例：
- `ledger balance --subject-id 2036268933803311110 --start 2021-01 --end 2021-01`
- `ledger balance --subject-id 2036268933811699715 --start 2021-01 --end 2021-01`

两次返回 JSON 完全一致：
- 都是 `19` 行
- 同时包含 `1122`、`1405` 等多个无关科目

同时：
- `ledger balance --start 2021-01 --end 2021-01 --level 1`
- `ledger balance --start 2021-01 --end 2021-01 --level 2`

两次返回也完全一致，`--level 2` 的前几条结果里仍然全是 `grade = 1`。

根因：
这个问题有两层：

第一层，CLI 传参就不对：
- CLI 把级次参数写成了 `level`
- 但后端余额表查询 BO 实际识别的是 `minLevel / maxLevel`

因此 `--level` 从命令层开始就没有映射到后端真正使用的字段。

第二层，后端余额表过滤逻辑也没有使用 `subjectId`：
- `querySubjectBalance()` 会先算出整张余额表
- 最终 `filterSubjectBalance()` 只按
  - 起止科目编号
  - 级次
  - 编码区间
  过滤
- 完全没有对 `queryBO.subjectId` 做过滤

所以即使 CLI 正确传了 `subjectId`，后端也会忽略它。

代码位置：
- CLI 余额表请求体构造，错误传 `level`：
  `agent-harness/cli_anything/wukong/core/ledger.py:70-94`
- 后端余额表入口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:156-160`
- 后端余额表实现，未按 `subjectId` 过滤：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:643-706`

结论：
当前 `ledger balance` 的科目筛选和级次筛选都不可信。业务上会让用户误以为自己在看“某一个科目”或“某一级科目”的余额，实际上看到的仍然是整张余额表。

19. 辅助核算卡片删空后，父辅助核算仍然无法删除

现象：
当前自定义辅助核算分类会进入一种“删不掉”的状态：
- 先新增一个自定义辅助核算分类
- 再新增一张卡片
- 删除这张卡片后，`adjuvant carte list` 已返回空数组
- 但继续删除父辅助核算时，接口仍报：
  - `删除失败！待删除的核算含有明细项目，无法删除，请先删除明细项!`

已实测：
- 临时创建过一条自定义辅助核算和一张卡片
- 卡片删除后列表为空，但父分类仍无法删除
- 为避免污染账套，测试数据已直接从本地数据库清理

根因：
辅助核算卡片删除并不是物理删除，而是软删：
- `FinanceAdjuvantCarteServiceImpl.deleteById()` 只是把 `status` 更新为 `3`

但删除父辅助核算时：
- `FinanceAdjuvantServiceImpl.deleteById()` 查询子卡片时
- 只按 `adjuvantId` 查询
- 没有过滤 `status != 3`

于是已经软删的卡片仍会被当成“还存在的明细项”，导致父分类永远删不掉。

代码位置：
- 卡片删除，实际只是软删：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:143-168`
- 父辅助核算删除时，未过滤软删卡片：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantServiceImpl.java:132-144`

结论：
当前辅助核算删除链路存在状态口径不一致问题。业务上会造成测试数据、误建分类和废弃辅助核算无法清理，账套会逐渐积累脏数据。

20. `adjuvant carte list --page-size` 不生效，总是按默认 15 条分页

现象：
卡片列表命令支持 `--page-size`，但无论传多少，返回结果里的分页大小都固定是 `15`。

已实测样例：
- `adjuvant carte list --adjuvant-id 2036269088271138816 --page-size 1`
- `adjuvant carte list --adjuvant-id 2036269088271138816 --page-size 10`

两次返回中的：
- `pageSize = 15`

说明 CLI 传入的分页大小没有被后端识别。

根因：
CLI 发送的请求体是：
- `pageNo`
- `pageSize`

但后端分页基类 `PageEntity` 识别的是：
- `page`
- `limit`

因此 `pageSize` 这个字段不会进入后端真实分页对象，最终自动回落到默认值 `15`。

代码位置：
- CLI 卡片列表请求体构造：
  `agent-harness/cli_anything/wukong/core/adjuvant.py:42-65`
- 后端分页基类，只识别 `page / limit`：
  `common/common-web/src/main/java/com/kakarote/core/entity/PageEntity.java:18-57`

结论：
当前 `adjuvant carte list --page-size` 不可信。业务上会导致卡片较多时无法可靠地分批拉取、分页浏览或做脚本化批量处理。

21. `statement reopen` 会假成功，且不校验传入日期是否真的可反结账

现象：
当前反结账命令即使没有实际反结账成功，也会返回成功提示。

已实测样例：
- 在临时账套 `2037908059828916224` 上先执行 `statement status`
- 再执行 `statement reopen --date 2099-12-01`
- CLI 返回：
  - `{"reopened": true, "date": "2099-12-01"}`
- 但再次执行 `statement status`，前后状态完全没有变化

说明：
- 当前命令层会把“没有发生任何状态变化”的情况也当成成功返回
- 传入的日期也没有真正参与“是否能反结账该期间”的判断

根因：
后端反结账分支没有按传入日期校验目标期间，只是简单删除“当前账套最新的一条结账记录”：
- 如果存在结账记录，就删除最新一条
- 如果不存在结账记录，也不会报错，直接正常返回

因此会出现两类误导：
- 反结账目标期间和传入日期不一致
- 实际没有任何反结账动作，但接口仍显示成功

代码位置：
- 后端 `queryStatement()` 只是按最新结账记录推导当前结账状态：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceStatementServiceImpl.java:146-161`
- 后端 `statement()` 反结账分支未校验日期，也未在无记录时报错：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceStatementServiceImpl.java:309-376`

结论：
当前 `statement reopen` 的成功返回不可信。业务上会让用户误以为账期已经打开，进而继续改凭证、补凭证或重走结账流程，最终造成账期状态认知错误。

22. `certificate list` 的 `--page` 与 `--size` 都不生效

现象：
凭证列表命令虽然支持分页参数：
- `--page`
- `--size`

但这两个参数在当前实现中都没有真正生效。

已实测样例：
- `certificate list --start 2021-01 --end 2021-01 --page 1 --size 1`
- `certificate list --start 2021-01 --end 2021-01 --page 1 --size 5`
- `certificate list --start 2021-01 --end 2021-01 --page 2 --size 5`

三次返回结果表现一致：
- 都返回 `15` 条记录
- 第 1 页和第 2 页首尾 `certificateId` 完全一致

说明：
- `--size` 没有改变每页条数
- `--page` 也没有真正翻页

根因：
CLI 发送的请求体是：
- `pageNo`
- `pageSize`

但后端凭证查询 BO 继承的分页基类 `PageEntity` 实际识别的是：
- `page`
- `limit`

因此凭证分页参数不会进入后端真实分页对象，最终自动回落到默认：
- `page = 1`
- `limit = 15`

代码位置：
- CLI 凭证列表请求体构造：
  `agent-harness/cli_anything/wukong/core/certificate.py:99-130`
- 后端凭证筛选 BO 继承分页基类：
  `finance/finance-web/src/main/java/com/kakarote/finance/entity/BO/FinanceSearchCertificateBO.java:25-40`
- 后端分页基类只识别 `page / limit`：
  `common/common-web/src/main/java/com/kakarote/core/entity/PageEntity.java:18-57`

结论：
当前 `certificate list` 的分页不可信。业务上会导致凭证较多时无法可靠翻页、分批核对、分批导出或脚本化处理，而且用户会误以为自己已经查看了后续页的数据。

23. `adjuvant add --label` 语义失真，传入标准标签后仍按“自定义”落库

现象：
辅助核算新增命令支持 `--label`，并且命令帮助里明确说明：
- `1=客户`
- `2=供应商`
- `3=职员`
- `4=项目`
- `5=部门`
- `6=存货`
- `7=自定义`

但实际新增时，即使传入标准标签，后端也仍会按“自定义辅助核算”保存。

已实测样例：
- 执行：
  `adjuvant add --name 标签测试1774764954 --label 4`
- CLI 返回成功，且回显：
  - `label = 4`
- 但紧接着查后端 `queryCustomList`，新建记录实际为：
  - `adjuvantType = 0`
  - `label = 7`

说明：
- 命令层回显和后端真实落库语义不一致
- 用户以为自己建的是“项目”，实际系统存的是“自定义辅助核算”

根因：
CLI 确实把传入的 `label` 传给了后端，但后端新增辅助核算时会强制覆盖：
- `adjuvant.setLabel(AdjuvantTypeEnum.CUSTOM.getType())`

因此：
- CLI 的 `--label` 在新增场景下只是一个“表面参数”
- 后端真实保存始终是 `CUSTOM(7)`

代码位置：
- CLI 辅助核算新增请求体构造：
  `agent-harness/cli_anything/wukong/core/adjuvant.py:19-27`
- 后端新增辅助核算时强制把 `label` 改为 `CUSTOM`：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantServiceImpl.java:52-66`

结论：
当前 `adjuvant add --label` 的业务语义不可信。业务上会让用户误以为自己创建的是标准辅助核算分类，实际却被系统保存成自定义分类，后续在界面展示、导入模板、业务规则理解上都会产生偏差。

24. `subject add --parent-id` 不可用，CLI 无法正常新增二级科目

现象：
当前 CLI 新增顶级科目可以成功，但新增二级科目会直接失败。

已实测样例：
- 顶级科目新增成功：
  `subject add --code 9400 --name CLI顶级测试768400 --type 1 --direction 1`
- 二级科目新增失败：
  `subject add --code 151180 --name CLI层级测试768380 --type 1 --direction 1 --parent-id 2036268933815894017`

其中父科目 `2036268933815894017` 对应：
- `1511 长期股权投资`
- `type = 1`
- `category = 2`
- `balanceDirection = 1`

CLI 已经传入了与父科目一致的：
- `type = 1`
- `direction = 1`

但接口仍返回：
- `API error 7026: 科目与上级科目，科目类别或科目类型不一致，不能添加!`

根因：
后端新增子科目时会强校验：
- 子科目的 `type` 必须和父科目一致
- 子科目的 `category` 也必须和父科目一致

但 CLI 当前新增科目时只传了：
- `number`
- `subjectName`
- `type`
- `balanceDirection`
- `parentId`
- `currencyType`
- `isEnd`

没有传 `category`，也没有根据父科目自动继承 `category`。

因此：
- 顶级科目新增时没有父级校验，所以能成功
- 二级科目新增时进入父级一致性校验，但 `category` 为空，直接被后端拒绝

代码位置：
- CLI 科目新增请求体缺少 `category`：
  `agent-harness/cli_anything/wukong/core/subject.py:32-60`
- 后端新增子科目时强校验 `category/type` 与父科目一致：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceSubjectServiceImpl.java:115-123`

结论：
当前 `subject add --parent-id` 在 CLI 层基本不可用。业务上会导致用户无法通过 CLI 正常扩展现有科目体系，只能新增顶级科目，不能可靠新增二级明细科目。

25. `subject list` 的非 JSON 输出不可读，编码列为空且树形层级无法正常展示

现象：
`subject list` 的人类可读输出存在两类明显问题：

第一类，编码列为空：
- 执行：
  `subject list --type 1`
- 表格中的 `Code` 列几乎全部为空

第二类，树形展示失真：
- 执行：
  `subject list --type 1 --tree`
- 结果看起来和普通平铺列表几乎一样
- 即使有父子科目关系，层级感也非常弱，难以一眼区分树结构

根因：
CLI 展示逻辑 `_flatten_subjects()` 取编码时读的是：
- `subjectNumber`

但当前接口返回字段实际是：
- `number`

因此 `Code` 列会被渲染成空值。

同时，树形缩进被加在了 `Code` 列，而不是 `Name` 列：
- 当 `Code` 本身就是空时
- 即使有子节点，也无法通过展示看出层级关系

代码位置：
- CLI 科目列表渲染逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:452-466`

结论：
当前 `subject list` 的非 JSON 输出不可信。业务上会让用户在终端里几乎无法正确查看科目编码，也难以辨认树形科目层级结构。

26. 三张报表的非 JSON 输出只显示项目名称，不显示金额

现象：
以下三张报表在人类可读输出模式下都只打印 `name` 一列，不显示金额列：
- `report balance-sheet --period month --date 2021-01`
- `report income --period month --date 2021-01`
- `report cash-flow --period month --date 2021-01`

实测表现：
- 终端里只能看到“流动资产”“营业收入”“经营活动产生的现金流量”等标题项
- 看不到期初、本期、期末等任何金额

但对应 JSON 返回中，实际存在大量金额字段，例如：
- `initialPeriod`
- `startPeriod`
- `endPeriod`

说明：
- 后端报表数据本身并没有缺金额
- 问题出在 CLI 的表格列筛选逻辑

根因：
CLI 的 `_print_report_table()` 在自动挑选金额列时，只会保留字段名中包含：
- `balance`

但当前报表返回的主要金额字段并不叫 `*balance*`，而是：
- `initialPeriod`
- `startPeriod`
- `endPeriod`

因此最终打印时，只剩下了 `name` 一列。

代码位置：
- CLI 报表打印逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:933-944`

结论：
当前三张报表的非 JSON 输出基本不可用。业务上会导致用户在终端里只能看到报表项目名，却看不到任何金额结果，无法直接用于查阅、核对或汇报。

27. `statement status` 的非 JSON 状态列永远为空

现象：
执行：
- `statement status`

CLI 会打印：
- `Last settlement`
- `Certificates this period`
- `Statement items`

但 `Statement items` 表格里的 `Status` 一列始终为空。

已实测样例：
- `转出未交增值税`
- `计提地税`
- `计提所得税`
- `结转损益`

这几项都会显示名称，但状态列没有任何值。

根因：
CLI 状态页表格渲染时，只尝试从每条结账项里读取：
- `status`
- `statementStatus`

但当前接口返回结构里并没有这两个字段，因此最终渲染结果只能是空字符串。

代码位置：
- CLI 结账状态展示逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:1250-1272`

结论：
当前 `statement status` 的非 JSON 展示不完整。业务上会让用户只能看到有哪些结账项，却看不到每一项当前到底是否已生成、是否已完成或是否可执行。

28. `certificate list` 的非 JSON 金额列失真，列表中大量显示 `None`

现象：
执行：
- `certificate list --start 2021-01 --end 2021-01 --page 2 --size 5`

CLI 表格会显示：
- `Debit`
- `Credit`

但这两列在实测结果里基本全是：
- `None`

说明：
- 从用户视角看，凭证列表像是“没有借贷金额”
- 但同一批记录的 JSON 返回里其实存在：
  - `total`
  - 明细行里的 `debtorBalance / creditBalance`

根因：
CLI 列表展示时直接取凭证头上的：
- `debtorBalance`
- `creditBalance`

而当前列表接口返回的这两个顶层字段大量为 `null`，因此表格会直接打印 `None`。

也就是说：
- 命令层选用了一个当前接口并不稳定提供的展示字段
- 但没有回退到更可读的 `total` 或其他可展示金额

代码位置：
- CLI 凭证列表表格渲染逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:602-639`

结论：
当前 `certificate list` 的非 JSON 展示不适合直接查阅金额。业务上会让用户误以为凭证没有借贷金额或列表数据异常，影响终端环境下的快速核对。

29. `account switch` 切不存在的账套也会报成功，并把本地会话写成无效账套

现象：
执行一个根本不存在的账套 ID：
- `account switch 999999999999999999`

CLI 仍然返回成功：
- `{"account_id": 999999999999999999, "switched": true}`

同时，本地会话文件 `~/.cli-anything-wukong/session.json` 会被真的写成：
- `account_id = 999999999999999999`

说明：
- 命令层会把“切换到一个不存在账套”的场景也当成成功
- 本地后续状态显示和命令上下文都会被污染

根因：
这个问题有两层：

第一层，后端切账套时没有校验账套是否存在：
- `switchAccountSet(accountId)` 直接执行默认账套切换
- 然后 `AccountSet.setAccountSet(getById(accountId))`
- 如果账套不存在，也没有抛出明确错误

第二层，CLI 只要接口没有报错，就无条件把本地 session 的 `account_id` 改成传入值。

代码位置：
- CLI 切换账套后无条件写本地 session：
  `agent-harness/cli_anything/wukong/wukong_cli.py:373-385`
- CLI 调后端切账套接口：
  `agent-harness/cli_anything/wukong/core/account.py:27-29`
- 后端切账套未做存在性校验：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAccountSetServiceImpl.java:443-455`

结论：
当前 `account switch` 的成功返回不可信。业务上会让用户误以为自己已切到目标账套，实际却把本地 CLI 上下文切进一个无效账套状态。

30. 日级日期没有真实自然日校验，非法日期会被系统接受并滚到其他日期

现象：
当前多个使用 `YYYY-MM-DD` 的命令都没有校验“这一天是否真实存在”。

已实测样例：
- `certificate add --date 2021-02-31 ...`
  - CLI 成功创建凭证
  - 查询后实际落库时间变成：
    - `2021-03-03 00:00:00`
- `certificate update --date 2021-02-31 ...`
  - CLI 成功更新
  - 查询后凭证日期同样被改成：
    - `2021-03-03 00:00:00`
- `certificate next-num --date 2021-02-31`
  - 直接正常返回 `1`
- `statement reopen --date 2021-02-31`
  - 也会直接显示成功

说明：
- 当前系统不只是“格式校验不足”
- 而是会把一个不存在的自然日当成合法输入，并转换成另一个真实日期

根因：
CLI 虽然定义了 `_DATE_RE`，但并没有真正用于这些日级日期命令的自然日校验。

当前命令层对以下命令都没有做“真实日期”验证：
- `certificate next-num`
- `certificate add`
- `certificate update`
- `statement close`
- `statement reopen`

因此非法日期会直接进入后端，后端解析时再发生日期滚动。

代码位置：
- CLI 只定义了 `_DATE_RE`，但没有统一用于这些命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:60-74`
- 未校验日期的凭证号查询：
  `agent-harness/cli_anything/wukong/wukong_cli.py:747-763`
- 未校验日期的凭证更新：
  `agent-harness/cli_anything/wukong/wukong_cli.py:766-789`
- 未校验日期的结账/反结账：
  `agent-harness/cli_anything/wukong/wukong_cli.py:1277-1305`

结论：
当前日级日期输入不可信。业务上会导致用户录入一个错误日期后，系统悄悄把凭证或状态改到另一个日期，造成账期错位和难以察觉的数据污染。

31. 不存在的对象经常被报成“网络错误”，错误语义失真

现象：
以下“对象不存在”的命令，当前都会报：
- `API error 500: 网络错误，请稍候再试`

已实测样例：
- `certificate get 999999999999999999`
- `voucher delete 999999999999999999`
- `adjuvant delete 999999999999999999`

说明：
- 这些场景都不是网络故障
- 但 CLI 最终给用户的却是“网络错误”提示

根因：
后端在对象不存在时，没有返回明确的“未找到”业务错误，而是走到了空指针或通用 500：

例如凭证详情：
- `getById(certificateId)` 为空后
- 后面仍然继续访问 `certificate.getCertificateId()`

类似地，凭证字删除、辅助核算删除也都会先 `getById(id)`，再直接取对象字段，没有先做存在性校验。

代码位置：
- 不存在凭证详情会继续访问空对象：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:388-401`
- 不存在凭证字删除会继续访问空对象：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceVoucherServiceImpl.java:95-104`
- 不存在辅助核算删除会继续访问空对象：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantServiceImpl.java:134-149`

结论：
当前部分命令的错误提示不可信。业务上会让用户在“对象不存在”和“系统网络故障”之间误判问题性质，增加排查成本。

32. `subject delete` 删除不存在的科目也会报成功

现象：
执行：
- `subject delete 999999999999999999`

CLI 会直接返回成功：
- `{"deleted": [999999999999999999]}`

说明：
- 当前命令不会告诉用户“这个科目其实不存在”
- 而是把一次“空删除”包装成了成功删除

根因：
后端 `deleteByIds(ids)` 的处理方式是：
- 先 `listByIds(ids)`
- 如果查询结果为空，也不会报错
- 后面直接执行 `removeByIds(ids)` 并返回成功

因此只要没有命中“被凭证使用/被初始余额使用”等阻断条件，就算目标根本不存在，也会被当成删除成功。

代码位置：
- 后端科目删除实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceSubjectServiceImpl.java:537-557`

结论：
当前 `subject delete` 的成功返回不可靠。业务上会让用户误以为科目已被成功删除，实际上可能只是删除了一个根本不存在的 ID。

33. `account list` 的非 JSON 输出字段错位，名称和启用日期显示为空

现象：
执行：
- `account list`

CLI 人类可读表格中：
- `Name` 列为空
- `Start Date` 列为空

但同一接口 JSON 返回中，实际存在：
- `companyName`
- `startTime`
- `enableTime`

说明：
- 账套数据本身不是空
- 问题出在 CLI 展示取字段时读错了字段名

根因：
CLI 当前读的是：
- `accountName / name`
- `startDate / enableDate`

而接口实际返回的是：
- `companyName`
- `startTime / enableTime`

因此人类可读输出就会出现字段空白。

代码位置：
- CLI 账套列表展示逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:341-367`

结论：
当前 `account list` 的非 JSON 输出不完整。业务上会让用户在终端里看不到账套名称和启用时间，影响账套识别与切换判断。

34. `ledger multi-column` 的非 JSON 输出会把有数据的结果显示成“无数据”

现象：
执行：
- `ledger multi-column --subject-id 2036268933841059866 --start 2021-01 --end 2021-01`

普通输出模式下，CLI 显示：
- `No data`

但同一个命令在 JSON 模式下实际返回了完整结果：
- `subjects`
- `jsonObjects`

说明：
- 后端确实返回了多栏账数据
- 只是 CLI 的非 JSON 展示没有把正确字段取出来

根因：
CLI 在非 JSON 渲染时只尝试读取：
- `result.get("list")`
- `result.get("data")`

而当前多栏账接口真实返回的是：
- `jsonObjects`

因此即使后端有数据，CLI 也会误判成空结果。

代码位置：
- CLI 多栏账展示逻辑：
  `agent-harness/cli_anything/wukong/wukong_cli.py:899-915`

结论：
当前 `ledger multi-column` 的非 JSON 输出不可信。业务上会让用户误以为某科目的多栏账没有数据，实际却只是命令展示层漏了结果。

35. `certificate review` 审核不存在的凭证也会报成功

现象：
执行一个根本不存在的凭证 ID：
- `certificate review 999999999999999999 --approve`

CLI 仍然返回成功：
- `{"action": "approved", "ids": [999999999999999999]}`

说明：
- 当前命令层会把一次“根本没有命中任何凭证”的审核操作当成成功
- 用户会误以为目标凭证已经被审核

根因：
后端审核前先做“借贷平衡校验”：
- `queryBalanceByIds(ids, accountId)`

但这段 SQL 在 `ids` 对应不到任何凭证详情时，`sum(...)` 会走空结果路径，最终仍可能返回：
- `1`

于是不会触发“不能审核”的异常。

后续：
- `update ... where certificate_id in (...)`
- 即使 0 行命中，也不会报错
- CLI 看到接口成功返回后，直接按审核成功处理

代码位置：
- CLI 审核接口调用：
  `agent-harness/cli_anything/wukong/core/certificate.py:258-267`
- 后端审核实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:299-320`
- 后端借贷平衡校验 SQL：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceCertificateMapper.xml:1504-1511`

结论：
当前 `certificate review` 的成功返回不可信。业务上会让用户误以为某张凭证已被审核，实际该凭证可能根本不存在。

36. `adjuvant carte update` 更新不存在的卡片也会报成功

现象：
执行：
- `adjuvant carte update 999999999999999999 --adjuvant-id 2036269088271138816 --name 不存在卡片`

CLI 直接返回成功：
- `{"updated": 999999999999999999}`

说明：
- 当前命令不会告诉用户“目标卡片不存在”
- 而是把一次 0 行命中的更新操作包装成了成功

根因：
后端卡片更新分支在校验编号冲突后，直接执行：
- `updateById(adjuvantCarte)`

但没有校验：
- 目标 `carteId` 是否真实存在
- 本次更新是否真的影响到任何记录

因此：
- 只要没有触发其他业务异常
- 即使目标卡片不存在，也会正常返回

代码位置：
- CLI 卡片更新命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:1195-1218`
- 后端卡片更新实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:100-110`

结论：
当前 `adjuvant carte update` 的成功返回不可靠。业务上会让用户误以为某张卡片已经修改完成，实际可能根本没有任何数据被更新。

37. `certificate delete` 删除不存在的凭证会报 500“网络错误”

现象：
执行：
- `certificate delete 999999999999999999`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

说明：
- 这不是网络故障
- 而是“删除一个不存在凭证”时，系统没有返回明确的业务语义错误

根因：
后端删除凭证时：
- 先按 ID 查询凭证列表
- 如果为空，也不会直接返回“凭证不存在”
- 后面仍继续执行删除关联明细、删除辅助核算关联等后续流程

当前这条空路径没有被正确处理，最终暴露成通用 500。

代码位置：
- 后端凭证删除实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:325-363`

结论：
当前 `certificate delete` 在对象不存在场景下的错误语义失真。业务上会让用户把“凭证不存在”误判成“系统网络异常”。

38. `adjuvant carte delete` 删除不存在的卡片会报 500“网络错误”

现象：
执行：
- `adjuvant carte delete 999999999999999999`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

说明：
- 这同样不是网络问题
- 而是“删除不存在卡片”时后端空路径处理错误

根因：
后端删除卡片时：
- 先查关联关系
- 再查待删除卡片列表
- 如果卡片列表为空，也没有直接返回“卡片不存在”
- 后续删除路径没有对这种空结果做明确处理

最终用户侧只看到一个泛化的 500 网络错误。

代码位置：
- 后端卡片删除实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:145-168`

结论：
当前 `adjuvant carte delete` 在对象不存在场景下错误提示失真。业务上会让用户误判问题性质，增加定位成本。

39. `report income --period quarter/year` 的“本期数”仍按单月计算

现象：
我做了直接对照：
- `report income --period month --date 2021-03`
- `report income --period quarter --date 2021-03`

两者关键行完全相同，例如：
- `管理费用`：`monthValue=243.0`、`yearValue=1333.8`
- `四、净利润`：`monthValue=-322.1`、`yearValue=-1617.4`

但按业务口径：
- 2021 年一季报的“本期数”应该覆盖 2021-01 到 2021-03
- 不应该仍然等于 2021-03 单月数

再看年报：
- `report income --period year --date 2021-12`

返回：
- `管理费用`：`monthValue=0.0`、`yearValue=1333.8`
- `四、净利润`：`monthValue=0.0`、`yearValue=-1617.4`

说明：
- 季报当前期金额退化成了“本月数”
- 年报当前期金额退化成了“12 月单月数”
- 只有 `yearValue` 还保留了累计语义

根因：
CLI 构造报表请求体时，无论 `month / quarter / year`，都统一把：
- `fromPeriod`
- `toPeriod`

都设成同一个 `yyyyMM`。

例如：
- `quarter 2021-03` 实际发送的是 `fromPeriod=202103, toPeriod=202103`
- `year 2021-12` 实际发送的是 `fromPeriod=202112, toPeriod=202112`

后端利润表计算“本期数”时，直接按这段区间查询科目余额，所以当前期金额自然只剩单月口径。

代码位置：
- CLI 报表请求体构造：
  `agent-harness/cli_anything/wukong/core/report.py:12-22`
- CLI 利润表命令：
  `agent-harness/cli_anything/wukong/wukong_cli.py:977-998`
- 后端利润表按 `fromPeriod/toPeriod` 查询并计算：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceIncomeStatementReportServiceImpl.java:61-141`

结论：
当前 `report income --period quarter/year` 的“本期数”口径不可信。业务上会把季报、年报误算成单月利润表，导致季度/年度经营分析失真。

40. `status --json` 会直接泄露完整登录 token

现象：
执行：
- `status --json`

返回里直接包含：
- `"token": "b4de024e54c742eebabc764ab69616f5"`

说明：
- 这不是仅供程序内部使用的隐藏字段
- 而是直接暴露在标准输出里

如果用户：
- 把 CLI 输出重定向到日志
- 在 CI/CD、脚本、调试终端里打印状态
- 把 JSON 结果粘贴给别人排查问题

就会把当前登录会话 token 一起泄露出去。

根因：
CLI `status` 命令在 JSON 分支中直接把 session 里的 `token` 原样输出：
- `"token": token`

没有做：
- 掩码
- 省略
- 仅调试模式可见

代码位置：
- `status` 命令实现：
  `agent-harness/cli_anything/wukong/wukong_cli.py:237-252`

结论：
当前 `status --json` 存在明显的凭据泄露风险。业务上会增加会话被复制、被冒用、被日志留存的概率。

41. 账簿命令对“反向账期”处理不一致，甚至会返回误导性结果

现象：
我用同一组反向账期做了对照：
- `--start 2021-03 --end 2021-01`

结果不一致：
- `certificate list`：正常返回空数组
- `ledger detail`：返回 `API error 500: 网络错误，请稍候再试`
- `ledger general`：返回 `API error 500: 网络错误，请稍候再试`
- `ledger balance`：不报错，还会返回一整张余额表

而且 `ledger balance` 不是“空结果”，而是会生成带误导性的数值。例如：
- 正向区间 `2021-01 ~ 2021-03` 时，`5602 管理费用` 的 `debtorCurrentBalance=8025.54`
- 反向区间 `2021-03 ~ 2021-01` 时，`5602 管理费用` 的 `debtorCurrentBalance=0`，但 `debtorYearBalance=6691.74`

说明：
- 同一个“用户把起止期间输反了”的输入，不同命令各自乱处理
- 有的直接炸 500
- 有的悄悄给出一张“看起来正常但口径错误”的表

根因：
第一层，CLI 只校验：
- 日期格式是不是 `YYYY-MM`

但不会校验：
- `start <= end`

第二层，后端细账/总账链路会走 `BiTimeUtil.analyzeTypeTime()`。
这段代码在手工区间分支里直接按：
- `diffYear * 12 + diffMonth`

计算周期数，没有处理反向区间；一旦 `start > end`，后续时间列表构造就会进入异常路径，最终在用户侧暴露成通用 500。

第三层，余额表链路根本没有对反向账期做校验。
它仍会：
- 按 `toPeriod` 拉取“截止到当前账期”的明细
- 再用 `fromPeriod` 计算本期发生额

于是反向区间不会报错，而是产出一张口径失真的余额表。

代码位置：
- CLI 账簿命令只校验格式：
  `agent-harness/cli_anything/wukong/wukong_cli.py:831-922`
- 后端细账实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:439-485`
- 后端总账实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:489-540`
- 后端余额表实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:643-709`
- 公共时间区间分析：
  `common/common-web/src/main/java/com/kakarote/core/utils/BiTimeUtil.java:589-738`

结论：
当前账簿类命令在“起止期间输反”场景下不可靠。业务上既会制造假网络错误，也会让用户拿到一张口径错误却不自知的账表。

42. `certificate add` 允许单条分录“借贷同时有值”

现象：
我直接用一条非法分录做了新增测试：
- `certificate add --voucher-id 2036268933740396548 --date 2021-03-15 --detail '[{"subjectId":2036268933799116800,"digestContent":"CLI校验-同行借贷","debtorBalance":1,"creditBalance":1}]'`

CLI 直接新增成功，返回了真实 `certificateId`。

随后查询详情，明细行确实已经落库：
- `subjectNumber=1002`
- `debtorBalance=1.0`
- `creditBalance=1.0`

说明：
- 当前系统允许同一条分录同时填借方和贷方金额
- 这不符合正常凭证明细的业务约束

根因：
CLI 侧的 `validate_certificate_details()` 只校验：
- 有科目时不能“借贷都为 0”
- 借贷合计必须平衡

但没有校验：
- 单条分录是否“借贷互斥”

后端 `saveAndUpdate()` 也没有再做二次语义校验，而是直接把明细写入数据库。

代码位置：
- CLI 凭证明细校验：
  `agent-harness/cli_anything/wukong/core/certificate.py:18-93`
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:143-192`

结论：
当前 `certificate add` 允许录入结构非法的凭证明细。业务上会直接污染总账、明细账和后续报表口径。

43. `certificate add` 允许负数金额直接入账

现象：
我用负数借贷做了新增测试：
- `certificate add --voucher-id 2036268933740396548 --date 2021-03-15 --detail '[{"subjectId":2036268933799116800,"digestContent":"CLI校验-负数借方","debtorBalance":-1,"creditBalance":0},{"subjectId":2036268933841059866,"digestContent":"CLI校验-负数借方","debtorBalance":0,"creditBalance":-1}]'`

CLI 直接新增成功，返回了真实 `certificateId`。

随后查询详情，明细行确实已经落库：
- 第一行：`debtorBalance=-1.0`
- 第二行：`creditBalance=-1.0`

说明：
- 当前系统没有阻止负数借方、负数贷方
- 只要总借贷“数值上相等”，这类非法金额就能直接进账

根因：
CLI 明细校验会把金额转成 float 后直接参与合计比较，但没有做：
- 金额必须大于等于 0
- 负数金额禁止入账

后端保存时同样没有再做金额合法性校验。

代码位置：
- CLI 凭证明细校验：
  `agent-harness/cli_anything/wukong/core/certificate.py:10-93`
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:143-192`

结论：
当前 `certificate add` 允许负数分录直接写入。业务上会让账面金额出现反向污染，后续统计和审核结果都会失真。

44. 非法凭证可以被 `certificate review --approve` 正常审核通过

现象：
我先新增了一张非法凭证，明细只有一条，而且同一行同时存在：
- `debtorBalance=2`
- `creditBalance=2`

随后执行：
- `certificate review 2038165486138802176 --approve`

CLI 返回审核成功。  
再次查询列表与详情，`checkStatus=1`，说明后端确实把这张非法凭证审核通过了。

说明：
- 这不是单纯“录入阶段没拦住”
- 连审核阶段也不会把这种非法结构挡下来

根因：
后端审核前只做了一种检查：
- `queryBalanceByIds(ids, accountId)`

对应 SQL 只看：
- `sum(debtor_balance) - sum(credit_balance) == 0`

也就是说它只校验“总借贷平衡”，不会校验：
- 单条分录是否借贷互斥
- 金额是否为负
- 分录结构本身是否合法

所以只要汇总结果相等，非法凭证也能通过审核。

代码位置：
- CLI 审核命令：
  `agent-harness/cli_anything/wukong/core/certificate.py:258-267`
- 后端审核实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:299-320`
- 后端审核前平衡校验 SQL：
  `finance/finance-web/src/main/java/com/kakarote/finance/mapper/xml/FinanceCertificateMapper.xml:1504-1511`

结论：
当前 `certificate review` 不能保证“审核通过的凭证就是结构合法的凭证”。业务上会把本应拦截的坏账数据正式确认进账。

45. `certificate add` 不校验科目是否存在，不存在的 `subjectId` 也能直接入账

现象：
我做了直接测试：
- `certificate add --voucher-id 2036268933740396548 --date 2021-03-17 --detail '[{"subjectId":999999999999999999,"digestContent":"CLI校验-不存在科目","debtorBalance":3,"creditBalance":0},{"subjectId":2036268933841059866,"digestContent":"CLI校验-不存在科目","debtorBalance":0,"creditBalance":3}]'`

CLI 直接新增成功，返回了真实 `certificateId`。

随后按 ID 查询详情，第一条明细已经真实落库：
- `subjectId=999999999999999999`
- `subjectNumber=null`
- `subjectName=null`
- `debtorBalance=3.0`

说明：
- 当前系统不会拦截“分录引用了根本不存在的科目”
- 而是把这条坏引用直接写进凭证明细

根因：
CLI 侧只校验：
- `subjectId` 这个字段是否有值

并不会校验：
- 这个 `subjectId` 在账套里是否真实存在

后端 `saveAndUpdate()` 在保存凭证明细时，也没有做科目存在性校验，而是直接：
- `financeCertificateDetailService.save(certificateDetail)`

代码位置：
- CLI 凭证明细校验：
  `agent-harness/cli_anything/wukong/core/certificate.py:18-93`
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:143-192`

结论：
当前 `certificate add` 可以写入“悬空科目引用”的脏凭证。业务上会造成凭证明细无法正常关联科目，后续查询、汇总、报表和排错都会变得不可靠。

46. `certificate add` 传不存在的 `voucherId` 时会先落库，再对外报 500

现象：
我做了直接测试：
- `certificate add --voucher-id 999999999999999999 --date 2021-03-18 --detail '[{"subjectId":2036268933799116800,"digestContent":"CLI校验-不存在凭证字","debtorBalance":4,"creditBalance":0},{"subjectId":2036268933841059866,"digestContent":"CLI校验-不存在凭证字","debtorBalance":0,"creditBalance":4}]'`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

但随后查询 2021-03 的凭证列表，能看到这张凭证已经真实存在：
- `voucherId=999999999999999999`
- `voucherName=null`
- `voucherNum=1`
- `total=4.00`

说明：
- 这不是“新增失败”
- 而是“数据已经写进库了，但响应阶段又炸了”

根因：
后端 `saveAndUpdate()` 保存凭证时，不校验 `voucherId` 是否存在，所以会先把凭证和明细写入库。

随后控制器在组装操作日志时执行：
- `financeVoucherService.getById(certificate.getVoucherId())`

对不存在的凭证字，返回值为 `null`；继续访问：
- `voucher.getVoucherName()`

就会触发空指针，最终在客户端表现成通用 500。

代码位置：
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:143-192`
- 新增接口响应阶段操作日志：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:74-84`

结论：
当前 `certificate add` 在不存在凭证字场景下会制造“假失败真落库”。业务上会让用户误以为新增没成功，随后重复操作，最终把脏凭证和重复凭证带进账套。

47. `certificate update` 传不存在的 `voucherId` 时会先更新落库，再对外报 500

现象：
我先新建了一张正常临时凭证，然后执行更新：
- `certificate update --id 2038169106859147264 --voucher-id 999999999999999999 --date 2021-03-20 --detail '[{"subjectId":2036268933799116800,"digestContent":"CLI更新链路-坏凭证字","debtorBalance":8,"creditBalance":0},{"subjectId":2036268933841059866,"digestContent":"CLI更新链路-坏凭证字","debtorBalance":0,"creditBalance":8}]'`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

但紧接着查询详情和列表，能看到这张凭证其实已经被更新成坏数据：
- `voucherId=999999999999999999`
- `voucherName` 为空
- 明细摘要和金额也都变成了更新后的值

说明：
- 这不是“更新失败”
- 而是“更新已经生效，但响应阶段又炸了”

根因：
后端更新接口与新增接口一样，先调用：
- `financeCertificateService.saveAndUpdate(financeCertificateBO)`

把凭证头和明细更新落库。

随后控制器再去取凭证字：
- `financeVoucherService.getById(certificate.getVoucherId())`

当 `voucherId` 不存在时，返回 `null`；继续访问：
- `voucher.getVoucherName()`

就会触发空指针，最终在客户端表现成通用 500。

代码位置：
- 更新接口响应阶段操作日志：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceCertificateController.java:90-102`
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:175-192`

结论：
当前 `certificate update` 在不存在凭证字场景下会制造“假失败真更新”。业务上会让用户误以为修改没成功，随后重复编辑，最终把脏凭证带入账套。

48. `certificate update` 更新不存在的凭证会假成功，并写出孤儿分录

现象：
我做了直接测试：
- `certificate update --id 999999999999999999 --voucher-id 2036268933740396548 --date 2021-03-22 --detail '[{"subjectId":2036268933799116800,"digestContent":"CLI孤儿分录校验-1774772510","debtorBalance":11,"creditBalance":0},{"subjectId":2036268933841059866,"digestContent":"CLI孤儿分录校验-1774772510","debtorBalance":0,"creditBalance":11}]'`

CLI 直接返回成功：
- `{"updated": true, "certificate_id": 999999999999999999}`

随后我直接查询本地 MySQL，发现 `wk_finance_certificate_detail` 已经写入两条新明细：
- `certificate_id = 999999999999999999`
- 对应凭证头并不存在

说明：
- 这不是简单的“更新不存在对象却误报成功”
- 而是会真实写出没有凭证头的孤儿分录

根因：
后端更新分支先执行：
- `updateById(certificate)`

即使 0 行命中也不会报错。

随后仍继续：
- 删除旧明细
- 保存新明细

也就是在“凭证头根本不存在”的情况下，仍会把新的 `certificate_detail` 写入库。

代码位置：
- 后端凭证保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:175-213`
- CLI 更新命令：
  `agent-harness/cli_anything/wukong/core/certificate.py:227-246`

结论：
当前 `certificate update` 在不存在凭证 ID 场景下会制造“假成功 + 孤儿分录”。业务上会直接破坏凭证与分录的一致性，后续排查和修复成本都很高。

49. `certificate review --unapprove` 反审核后不会清空审核人

现象：
我新建一张临时凭证后，依次执行：
- `certificate review 2038169824299040768 --approve`
- `certificate review 2038169824299040768 --unapprove`

然后同时查询详情和列表，结果一致：
- `checkStatus=0`
- 但 `examineUserId=1`
- `examineUserName=管理员`

说明：
- 反审核后，凭证状态已经回到“未审核”
- 但审核人字段仍残留为上一次审核人

这会让一张“未审核凭证”看起来仍像被某人审核过。

根因：
后端审核状态更新逻辑里，无论是审核还是反审核，都统一执行：
- `set(FinanceCertificate::getExamineUserId, UserUtil.getUserId())`

也就是说反审核时并不会把审核人清空，而是继续写当前操作人。

代码位置：
- 后端审核实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceCertificateServiceImpl.java:299-305`

结论：
当前凭证反审核后的状态字段不干净。业务上会让“未审核”和“已审核过但被撤销”的边界混乱，影响凭证状态理解和后续追责判断。

50. `adjuvant carte add` 传不存在的 `adjuvantId` 时会先落库孤儿卡片，再对外报 500

现象：
我做了直接测试：
- `adjuvant carte add --adjuvant-id 999999999999999999 --number BADADD1774772558 --name CLI坏父辅助卡片新增1774772558`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

但随后直接查询本地 MySQL，发现 `wk_finance_adjuvant_carte` 已经插入真实记录：
- `adjuvant_id=999999999999999999`
- `carte_number=BADADD1774772558`
- `carte_name=CLI坏父辅助卡片新增1774772558`
- `status=1`

说明：
- 这不是“新增失败”
- 而是“卡片已经写进库了，只是响应阶段炸了”

根因：
后端 `saveAndUpdate()` 在新增分支会先执行：
- `save(adjuvantCarte)`

随后为了拼操作日志，再去查父辅助核算：
- `adjuvantService.getById(adjuvantCarte.getAdjuvantId())`

当父辅助核算不存在时，后续访问：
- `adjuvant.getAdjuvantName()`

触发空指针，最终对客户端表现成通用 500。

代码位置：
- 后端卡片保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:91-112`
- 卡片新增接口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceAdjuvantCarteController.java:60-65`

结论：
当前 `adjuvant carte add` 在不存在父辅助核算场景下会制造“假失败真落库”的孤儿卡片。业务上会让用户误以为没新增成功，实际却把脏卡片写进账套。

51. `adjuvant carte update` 把卡片改到不存在的 `adjuvantId` 时会先更新落库，再对外报 500

现象：
我先新建了一张正常卡片，然后执行更新：
- `adjuvant carte update 2038170048321011712 --adjuvant-id 999999999999999999 --name CLI更新后坏父卡片1774772581`

CLI 返回：
- `API error 500: 网络错误，请稍候再试`

但随后查询本地 MySQL，这张卡片已经被真实更新成坏数据：
- `adjuvant_id=999999999999999999`
- `carte_name=CLI更新后坏父卡片1774772581`

说明：
- 这不是“更新失败”
- 而是“更新已生效，但响应阶段又炸了”

根因：
后端卡片更新分支先执行：
- `updateById(adjuvantCarte)`

把数据更新落库。

随后仍会用更新后的 `adjuvantId` 去查父辅助核算并拼日志：
- `adjuvantService.getById(adjuvantCarte.getAdjuvantId())`

当父辅助核算不存在时，继续访问：
- `adjuvant.getAdjuvantName()`

就会触发空指针，对外表现为通用 500。

代码位置：
- 后端卡片保存实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:91-112`
- 卡片更新接口：
  `finance/finance-web/src/main/java/com/kakarote/finance/controller/FinanceAdjuvantCarteController.java:68-73`

结论：
当前 `adjuvant carte update` 在不存在父辅助核算场景下会制造“假失败真更新”。业务上会把正常卡片直接改坏，并误导用户继续重复操作。

52. `excelImport` 类导入接口当前整体不可用，multipart 解析阶段直接 500

现象：
我分别直接测试了两条导入接口：
- `POST /financeAdjuvantCarte/excelImport`
- `POST /financeSubject/excelImport`

两者都使用了本系统导出模板对应的 `xls` 文件格式，并携带了有效 token。

无论是合法父辅助核算，还是正常科目导入样例，接口都直接返回：
- `{"code":500,"msg":"网络错误，请稍候再试","data":null}`

同时数据库里没有落入任何导入数据，说明请求在真正业务导入前就已经失败。

根因：
这不是业务校验报错，而是服务端在解析 multipart 上传时就失败了。

本地日志里有明确异常：
- `java.nio.file.FileSystemException: /mnt: Read-only file system`

配置文件里可以看到 multipart 临时目录被写死在：
- `/mnt/upload/`

当前运行环境下 `/mnt` 是只读文件系统，导致 Undertow 在解析上传文件、尝试创建临时目录时直接抛异常，最终对客户端统一表现成 500。

代码位置：
- multipart 临时目录配置：
  `common/common-web/src/main/resources/application-core.yml:13-17`
- 本地服务日志：
  `logs/finance.log`

结论：
当前所有依赖 multipart 上传的导入接口都处于不可用状态。业务上会直接阻断科目导入、辅助核算卡片导入等批量导入能力。

53. `financeSubject/deleteByIds` 混合真实 ID 和假 ID 时会静默“部分成功”

现象：
我先新建了一条临时顶级科目，然后直接调用后端批量删除接口，传入：
- 一个真实存在的 `subjectId`
- 一个不存在的 `999999999999999999`

接口返回成功：
- `{"code":0,"msg":"success","data":null,"operationLogs":null}`

随后查询数据库，真实科目已经被删除，不存在的 ID 则被静默忽略，接口没有任何“部分成功”或“部分失败”的提示。

说明：
- 这不是“整批删除成功”
- 而是“真实存在的删掉了，不存在的直接跳过了”

根因：
后端实现里先：
- `listByIds(ids)`

因此只能取到真实存在的科目。

随后直接：
- `removeByIds(ids)`

对不存在的 ID 不会报错，也不会补充错误信息，最终客户端只能看到一条整体成功的返回。

代码位置：
- 后端科目批量删除实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceSubjectServiceImpl.java:537-559`

结论：
当前 `financeSubject/deleteByIds` 的批量删除回执不可信。业务上会让操作者误以为整批科目都删掉了，实际上只有一部分生效。

54. `financeAdjuvantCarte/exportExcel` 成功返回但导出空文件

现象：
我直接调用了导出接口：
- `POST /financeAdjuvantCarte/exportExcel`，参数 `{"adjuvantId":2036269088271138816}`

虽然 HTTP 200，`Content-Disposition` 和 `fileData` header 也在，但下载文件是空的：
- `Content-Length: 0`
- 本地保存后的文件 `file` 也报告 `empty`

说明：
- 响应确实输出了“成功”的下载头，但没有写入任何 Excel 内容

根因：
导出实现：
- 把数据写到 `excelData/<uuid>.xlsx`
- 用 `BigExcelWriter` 写好数据
- 然后直接调用 `writer.close()` 和 `IoUtil.close(out)`，从未把文件流写到 `response` 本身

换句话说：文件生成在本地，HTTP 输出流被立即关闭，客户端拿不到表格内容。

代码位置：
- 导出实现：
  `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceAdjuvantCarteServiceImpl.java:390-423`

结论：
当前导出接口虽然声明可用，但实际响应体为空。业务上会让下载机制失效，用户看起来像“导出成功”，但拿不到任何数据。

55. `financeVoucher/sort` 无法改变排序

现象：
我先查询凭证字列表，所有返回的 `sort` 均为 `null`。
然后执行：
- `POST /financeVoucher/sort?voucherStartId=2036268933740396544&voucherEndId=2036268933740396548`
再次查询列表，顺序和 `sort` 全都没有变化。

说明：
- 接口返回 200，但排序看上去并无任何效果

根因：
排序实现只是交换 `sort` 字段的值：
```
sort = start.sort
start.sort = end.sort
end.sort = sort
```
当两个字段初始都是 `null` 时，交换后还是 `null`，因此根本无法把顺序写入数据库。

代码位置：
- `finance/finance-web/src/main/java/com/kakarote/finance/service/impl/FinanceVoucherServiceImpl.java:109-118`

结论：
当前凭证字排序功能无法运行。业务上会让用户以为自己调整了凭证字顺序，但数据库里无变化。
